pipeline { 
    agent any
     stages {
         stage('Build') {
             steps { 
                sh "Hello !! This is test" }
                }
            }
                 post { 
                    success { echo "Build is successfull" } 
                    failure { echo "Build failed" }
                     }
                      }
